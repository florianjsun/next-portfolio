- 自定义 Hook 以 `use-` 前缀命名并通过 `export function` 暴露纯函数形式
- 全局状态通过 Zustand 的 `create` 在独立文件中定义 store 并导出 hook 供组件订阅
- Next.js 客户端 Provider 使用 `'use client'` 指令并在首行声明，通过 `isMounted` 状态规避 SSR 水合不一致
- 工具函数保持无副作用并以命名导出暴露，类型通过接口（如 `BlogFrontmatter`、`BlogMeta`）集中声明