- 每篇博文顶部使用 YAML front matter 声明 title、date、description、tags、coverImage、featured、readingTime 等元数据字段。
- 正文遵循 Markdown 规范，使用 `##` / `###` 分节组织步骤、技术栈、项目结构等内容块。
- 代码片段以带语言标识的 fenced code block（bash、js、typescript、html、css）嵌入，便于站点语法高亮。
- 外部资源通过绝对路径引用（如 `/projects/niya/logo.png`），与站点 public 目录对齐。