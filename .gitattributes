# Shell scripts run on the Linux server. A CRLF checkout breaks the shebang
# ("bad interpreter: /usr/bin/env bash^M"), so pin them regardless of the
# contributor's core.autocrlf setting.
*.sh text eol=lf
