该模块为纯静态内容目录 `content/blogs/`，不包含任何代码或构建逻辑，仅由站点的博客渲染层（如 Next.js MDX/Markdown 解析器）消费。每篇 `.md` 文件采用统一的 YAML front matter 结构（title、date、description、tags、coverImage、featured、readingTime），正文使用标准 Markdown 编写，包含代码块、链接与分节标题；站点通过读取此目录下所有 `.md` 文件并按 front matter 中的 date/featured/tags 等字段排序、筛选、展示到博客列表与详情页。