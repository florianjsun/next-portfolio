- 所有源文件通过 tsconfig.json 中配置的 @/* 路径别名进行跨模块引用，避免相对路径嵌套。
- TypeScript 严格模式（strict + isolatedModules + noEmit）在全站生效，所有 .ts/.tsx 文件参与同一编译上下文。
- 代码风格由根目录的 eslint.config.mjs 与 .prettierrc 统一约束，lint 与 prettier 在单仓层面一次性运行。