该模块为纯静态内容目录 `content/blogs/`，不包含任何代码逻辑，仅由若干 `.md` 文件组成，每个文件代表一篇博客文章。文章统一采用 YAML Front Matter（`title`、`date`、`description`、`tags`、`coverImage`、`featured`、`readingTime`）作为元数据声明，供上层渲染层（如 Next.js 的 MDX/Markdown 解析器）读取并生成页面；正文使用标准 Markdown，可嵌入代码块、表格、外链等富文本。各文章之间相互独立，无内部依赖关系，通过文件名 slug 映射到路由。